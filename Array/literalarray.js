// Empty array literal
const emptyList = []; 

// Array of strings
const fruits = ["Apple", "Banana", "Cherry"]; 

// Mixed data types (Strings, numbers, booleans, objects)
const mixedArray = ["John Doe", 28, true, { hometown: "New York" }]; 

console.log(emptyList);
console.log(fruits);
console.log(mixedArray);